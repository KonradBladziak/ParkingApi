@model IEnumerable<DAL.Entity.Miejsce>

@if (Model.Count() > 0)
{
    <a asp-action="Create">Dodaj miejsce</a>
    <hr />
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.ParkingId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MiejsceInwalidzkieId)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ParkingId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MiejsceInwalidzkieId)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edytuj</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Usuń</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>Brak miejsc!</p>
}
<a asp-action="Index">Powrót</a>
<hr />

